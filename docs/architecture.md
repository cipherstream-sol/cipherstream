# Architecture (high-level)

CipherStream focuses on a simple execution flow:

User intent → Encrypted route selection → Guardrail checks → On-chain settlement → Telemetry surface

The public interface highlights routing, controls, and visibility as the core primitives.
